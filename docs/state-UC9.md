# Union-Closed Compatibility-Geometry — UC9 cumulative state ledger

**Ticket:** mg-96a6 — UC9, the first cross-cathedral ticket on the union_closed line: consume the unconditional F17+F18 cohomology of $\Delta(\mathrm{PPF}_n)$ as a black box, embed the Boolean lattice $2^{[n]}$ into $\mathrm{PPF}_{n+1}$ via the F18-natural cone embedding $S\mapsto\omega_S$, pull back / read off constraints on an intersection-closed family $\mathcal F$, attempt Frankl. **Parent:** Daniel Apple reminder 2026-05-15T23:57:20Z, articulating the next direction on the union_closed line; pm-onethird forwarded into UC slot post-UC8 and post-F28 AMBER. **Chain (UC line):** mg-f72a (UC1) → mg-a814 (UC2) → mg-cfc0 (UC3) → mg-0bf7 (UC4) → mg-3806 (UC5) → mg-6dad (UC6) → mg-a032 (UC7) → mg-d68d (UC8) → **mg-96a6 (UC9, this)**.
**Branch:** `polecat-cat-mg-96a6`.
**Deliverables:** `docs/union-closed-UC9-intersection-closed-flip-embedding-cohomology.md` (the main scoping doc — flip + embedding + cohomology pullback + three bridges + verdict + U1 check); `docs/state-UC9.md` (this file — cumulative state ledger).
**Type:** **Cross-cathedral construction** — first UC ticket consuming the F-series cohomology cathedral as load-bearing input. Specifically consumes the unconditional F17 (mg-4d3a) + F18 (mg-d039) theorem on $\Delta(\mathrm{PPF}_n)$ as a black box. **No new axioms; no Lean; no computation; no engine port.** Multi-session probable; this ledger survives session boundaries; a future session re-checks the invariants in §3 before extending. 300k cap (narrower than F29's 500k — UC9 is a specific embedding + specific application, not a framework search).

---

## Verdict (current): AMBER-cohomology-pulls-back-cleanly-but-Frankl-extraction-needs-UC10

The Daniel-articulated four-step strategy — *(1) flip Frankl to intersection-closed dual; (2) embed $2^{[n]}$ into $\mathrm{Pos}_{n+1}$ in a nice way; (3) consume F17+F18 unconditional top cohomology of $\mathrm{Pos}_{n+1}$; (4) extract constraints on the family* — lands its first three steps unconditionally and stalls structurally at the fourth on a **U1-shaped wall**. The wall: the embedded image $\mathrm{im}(\iota_n^{\mathrm{cone}})=(\omega_n\!\downarrow)\setminus\{\emptyset\}\subseteq\mathrm{PPF}_{n+1}$ is contractible (Boolean cube with maximum $\omega_n$), so the direct cohomology pullback $(\iota_n^{\mathrm{cone}})^*:\widetilde H^*(\Delta_{n+1})\to\widetilde H^*(\Delta(\mathrm{im}))=0$ is the **zero morphism** in every degree, and the family-pair LES is **family-independent on the contractibility class** (Theorem 4.5 — every intersection-closed $\mathcal F$ with $[n]\in\mathcal F$ produces the same relative class $\mathrm{sgn}_{S_n}$). Three candidate non-functorial bridges around the wall are surfaced with precise UC10 mandates: **A** vertex-slice Mayer–Vietoris on the principal cover $\{(\omega_n^{(x)}\!\downarrow)\}_{x\in[n]}$; **B** Möbius/Euler-characteristic with deletion-contraction (using $\mu_{\mathrm{PPF}_{n+1}^*}(\widehat 0,\widehat 1)=(-1)^{n-1}$ from F17+F18); **C** equivariant $\mathrm{sgn}_{S_n}$-isotypic projection on the boundary $(n-2)$-sphere of the punctured Boolean cube. All three respect Daniel's hard constraints (NOT factorial, NOT functorial). UC9 ranks them B → C → A for UC10 tractability.

AMBER not GREEN because step 4 is not landed: no Frankl bound is extracted unconditionally in this session; Bridges A/B/C are *set up* with concrete UC10 mandates but none is *executed* to closed form. AMBER not RED because the embedding is non-vacuous (uniquely pinned by Theorem 2.4), the cohomology pullback is *cleanly identified* as the U1-shaped wall (not a vague mismatch), and three independent dissolve-attempts are concrete and tractable.

The U1 wall is **structurally identical to the F-series U1 wall** (mg-26fc — "no operational bridge between BK spectral data and $\Delta(\mathrm{PPF}_n)$ cohomology"), in cohomology dialect: in both cases, the constructed object lands inside the F17+F18 cathedral but the cathedral's content does not couple to the constructed object via the *obvious* morphism. The dissolve in both dialects is the same: a non-functorial bridge that goes around the obvious morphism. UC9 surfaces three; F28/F29 surfaced one (Čech cohomology) on the 1/3-2/3 side. The two lines are sibling instantiations of the same meta-thesis (mg-26fc).

---

## Session 1 — 2026-05-16 (polecat mg-96a6) — DONE

**Goal:** Pin the Daniel-articulated UC9 strategy: precise embedding $2^{[n]}\hookrightarrow\mathrm{PPF}_{n+1}$; restate F17+F18 cohomology in the form the embedding consumes; pull back to identify constraints; surface the U1-obstruction; verdict + UC10 ask. Cross-cathedral but black-box-disciplined: F17+F18 only, no F-series internal engine touched.

| Item | Status | Output |
|---|---|---|
| Read parents on `union_closed`: UC1–UC8 docs + state ledgers; the UC1 §3/§4/§8.4 dead routes (E1–E3 / C1–C5) | ✅ | UC1 framework-vs-engine split, UC2 $m(\mathcal F)\le 2$, UC3 Walsh, UC4 large-fibre + I1, UC5 defect retraction + Gram, UC6 deficiency contraction Thm 3.1, UC7 lever signings, UC8 cross-fibre persistence + cumulative cross-incidence + F-iii square-defect bound — all internalized; UC9 §0.1 respects all dead routes |
| Read F17 (mg-4d3a) + F18 (mg-d039) state docs and main docs (cross-repo `one_third_width_three`) | ✅ | F17 Thm 5.1 ((UCC.1) ⟺ Hyp(n)), F17 reduction $\widetilde H_d(\Delta_{n+1}/\Delta_n)\cong 2\widetilde H_{d-1}(\Delta_n)$ unconditional; F18 cone $\omega_n=\{(n+1,b):b\in[n]\}$, F18 zig-zag $\iota_n\le\kappa_n\ge\mathrm{const}_{\omega_n}$, $\delta_n$ injective unconditional; F10 induction closes Hyp(n) for all $n$ — all internalized; UC9 §0.2 black-boxes as **Fact F**: $\widetilde H^*(\Delta_n;\mathbb Q)=\mathrm{sgn}_{S_n}$ in degree $n-2$, zero else, all $n\ge 3$ |
| Read mg-26fc U1-obstruction framing + structural-analogy meta-thesis | ✅ | "constrained compatibility geometry cannot remain globally isotropic" with F17+F18 as unconditional non-isotropy engine; U1-language inherited for UC9 §6.2 |
| Read mg-70b0 (F29) sibling scoping | ✅ | F29 = Čech-assembly mechanism on 1/3-2/3 side; UC9 = embedding-pullback mechanism on union_closed side; structurally distinct, both consume F17+F18, both screen out factoriality + functoriality; UC9 independent of F29 outcome |
| **§0 — Notation and the UC9 hand-off (UC1–UC8 cathedral + F-series cathedral + Daniel verbatim + hard constraints)** | ✅ | UC1–UC8 dictionary recap; F-series dictionary recap; Fact F at $n+1$; Daniel reminder 2026-05-15T23:57Z verbatim; hard constraints (NOT factorial, NOT functorial, paper-and-pencil, cumulative ledger, verify-embedding-directly per `feedback_n_poset_is_not_ordinal_sum`); F29 sibling relation; the UC9 target restated |
| **§1 — The intersection-closed flip** | ✅ | **Lemma 1.1** (the flip): $\mathcal F$ union-closed ⟺ $\mathcal F^c=\tau(\mathcal F)$ intersection-closed, $\beta_x(\mathcal F^c)=-\beta_x(\mathcal F)$; **Cor 1.2** (dual statements of Frankl); §1.2 the Frankl-relevant case (WLOG $[n]\in\mathcal F$, $\emptyset\in\mathcal F$); §1.3 the rarity statistics $f_x^c$; **Lemma 1.3** (trivial Frankl case: $\overline{\|A\|}\le n/2\Rightarrow$ avg $\ge|\mathcal F|/2$ trivially) — non-trivial Frankl regime is $\overline{\|A\|}>n/2$ on intersection-closed side |
| **§2 — The cone embedding $\iota_n^{\mathrm{cone}}:2^{[n]}\setminus\{\emptyset\}\hookrightarrow\mathrm{PPF}_{n+1}$** | ✅ | Definition $S\mapsto\omega_S=\{(n+1,b):b\in S\}$; **Lemma 2.1** well-defined ($\omega_S\in\mathrm{PPF}_{n+1}$ for $S\ne\emptyset$); **Lemma 2.2** order-preserving; **Lemma 2.3** lattice morphism (meet + join preserved); **Lemma 2.4** image = $(\omega_n\!\downarrow)\setminus\{\emptyset\}$; **Lemma 2.5** $S_n$-equivariant (with $S_n\subseteq S_{n+1}$ fixing $n+1$); §2.3 direct verification of antichain / 2-chain / N-poset / empty / total / cone $\omega_n$ structures (per `feedback_n_poset_is_not_ordinal_sum`); **Theorem 2.4** uniqueness — $\iota_n^{\mathrm{cone}}$ is the unique embedding satisfying $S_n$-equivariance + meet-preservation + $\iota([n])=\omega_n$ + image-is-principal-ideal jointly; alternatives surveyed (anti-cone, pinned-cone, filter-lattice, ordinal-sum) and dismissed; §2.5 the embedded family $\widetilde{\mathcal F}=\iota(\mathcal F\setminus\{\emptyset\})\subseteq(\omega_n\!\downarrow)$ as meet-subsemilattice with maximum $\omega_n$ |
| **§3 — F17+F18 cohomology of $\Delta(\mathrm{PPF}_{n+1})$ restated for UC9** | ✅ | §3.1 Fact F at $n+1$: $\widetilde H^k(\Delta_{n+1};\mathbb Q)=\mathrm{sgn}_{S_{n+1}}$ for $k=n-1$, zero else; §3.2 the degree-shift engine $\widetilde H^k(\Delta_{n+1})\cong\widetilde H^{k-1}(\Delta_n)\cong\widetilde H^{k-2}(\Delta_{n-1})\cong\cdots\cong\widetilde H^1(\Delta_3)$ (Daniel's "cohomology type is $\mathrm{Pos}_{n-1}$" unpacked: $n-1$ indexes the cohomological *degree*; the iterated F18-shift identifies the $\mathrm{Pos}_{n+1}$ top class with the $\mathrm{Pos}_{n-1}$ top class shifted up two degrees); §3.3 specific structural data **(D-1)**–**(D-5)** for §4 / §5 consumption (top-degree concentration + sgn; sgn-branching $S_{n+1}\to S_n$; principal-ideal $S_n$-stability; punctured-ideal $\simeq S^{n-2}$ with sgn-rep on top; principal-filter $(\omega_n\!\uparrow)$ structure) |
| **§4 — Pulling back to the embedded image (the U1 wall)** | ✅ | §4.1 **Lemma 4.1** the embedded image is contractible (Boolean cube with maximum $[n]$, equivalently $\omega_n$); **Cor 4.2** direct pullback $(\iota_n^{\mathrm{cone}})^*=0$ in every degree — **the U1 wall, in cohomology dialect**; §4.2 LES of the pair; **Theorem 4.3** the survival theorem ($\widetilde H^k(\Delta_{n+1},\Delta(\mathrm{im}))\cong\widetilde H^k(\Delta_{n+1})$, top class survives as relative class); §4.3 family-pair LES; **Lemma 4.4** family is contractible (cone with apex $\omega_n$); **Theorem 4.5** family-relative survival — *the relative cohomology is **family-independent on the contractibility class*** (every intersection-closed $\mathcal F$ with $[n]\in\mathcal F$ gives the same relative $\mathrm{sgn}_{S_n}$ class); §4.4 structural takeaway: family info not visible to relative cohomology in degree $n-1$, must be extracted by *complementary* / non-direct methods |
| **§5 — Constraints on $\mathcal F$ — the three bridges** | ✅ | §5.1 vertex slices $\omega_n^{(x)}=\omega_n\setminus\{(n+1,x)\}$; **Lemma 5.1** the principal cover $(\omega_n\!\downarrow)\setminus\{\omega_n\}=\bigcup_x(\omega_n^{(x)}\!\downarrow)$; **Lemma 5.2** Frankl in slice-statistics dialect ($\max_x f_x^c\ge|\widetilde{\mathcal F}|/2$); §5.2 **Bridge A** vertex-slice Mayer–Vietoris on principal cover (mandate for UC10: degeneration of MV spectral sequence under intersection-closure structural input); §5.3 **Bridge B** Möbius/Euler-characteristic deletion-contraction (using $\mu_{\mathrm{PPF}_{n+1}^*}(\widehat 0,\widehat 1)=(-1)^{n-1}$ from F17+F18; mandate for UC10: Crapo–Rota deletion-contraction recursion + closed-form vertex-slice localisation); §5.4 **Bridge C** equivariant $\mathrm{sgn}_{S_n}$-isotypic projection on $S^{n-2}$ (using sgn-branching + sgn-Euler-characteristic identity; mandate for UC10: equivariant Lefschetz-trace + averaging-over-intersection-closure); §5.5 dual case $[n]\notin\mathcal F$ trivial; §5.6 honest status — three bridges set up, none completed in UC9; all three respect NOT factorial / NOT functorial |
| **§6 — Verdict, U1 obstruction check, UC10 ask** | ✅ **AMBER-cohomology-pulls-back-cleanly-but-Frankl-extraction-needs-UC10** | §6.1 verdict; §6.2 U1 obstruction check — UC9 wall structurally identical to F-series U1 wall (contractible-probe phenomenon: any contractible probe sees zero through obvious pullback into F17+F18-concentrated cohomology); both dissolves are non-functorial bridges; §6.3 UC10 ask (pick bridge, run to closed form, verify hard-constraint compliance, extract Frankl bound); §6.4 bridge ranking B → C → A for UC10 tractability; §6.5 independence statements (UC9 ⟂ F29, UC9 ⟂ F-series program-status, UC9 ⟂ UC1–UC8 native cathedral); §6.6 trust-surface impact NONE |
| **§7 — references** | ✅ | parent chain UC1–UC8, cross-repo F17/F18/F10/F11/mg-26fc/F29, Frankl literature, cohomology/topology/Möbius background, directives consumed |
| Deliverable 1 — `docs/union-closed-UC9-intersection-closed-flip-embedding-cohomology.md` | ✅ | written |
| Deliverable 2 — `docs/state-UC9.md` | ✅ | this file |
| Trust-surface impact | ✅ none | paper-and-pencil; no axioms, no Lean, no computation; no engine port (E1–E3 / C1–C5 untouched); 1/3-2/3 critical path untouched; F-series cathedral consumed only via black-boxed Fact F (F17+F18 unconditional theorem) |

**What is proven vs. open in UC9.** *Proven (unconditional):* Lemmas 1.1, 1.3, 2.1, 2.2, 2.3, 2.4, 2.5, 4.1, 4.4, 5.1, 5.2; Cor 1.2, 4.2; **Theorem 2.4** (uniqueness of cone embedding under joint constraints); **Theorem 4.3** (survival theorem); **Theorem 4.5** (family-relative survival, family-independent on contractibility class); the precise statement of the U1-shaped wall in §4.4 + §6.2; the structural analogy with F-series U1 in §6.2; the bridge-ranking logic in §6.4. *Open (named for UC10 — three explicit bridges):* **(B)** Möbius/Euler-characteristic deletion-contraction on $\mathrm{PPF}_{n+1}^*$ relative to $\widetilde{\mathcal F}$, organised by vertex-slice cover, evaluated against $(-1)^{n-1}$; **(C)** equivariant $\mathrm{sgn}_{S_n}$-isotypic projection of equivariant Euler characteristic on $S^{n-2}$ relative to $\widetilde{\mathcal F}\setminus\{\omega_n\}$; **(A)** vertex-slice MV spectral sequence degeneration under intersection-closure structural input. Each bridge has a precise UC10 session-mandate (§§5.2–5.4); each respects Daniel's hard constraints. The 300k cap was not approached.

---

## Invariants (reproduce-on-resume)

Load-bearing facts for any future session. UC1–UC8 invariants (`docs/state-UC1.md` … `state-UC8.md`) remain in force; these are the UC9 additions.

### UC9 setup invariants

- **The flip (UC9 Lemma 1.1).** $\mathcal F$ union-closed on $[n]$ ⟺ $\mathcal F^c:=\{[n]\setminus A:A\in\mathcal F\}$ intersection-closed on $[n]$, $|\mathcal F^c|=|\mathcal F|$, $\beta_x(\mathcal F^c)=-\beta_x(\mathcal F)$. Frankl (union-closed) ⟺ Frankl (intersection-closed dual). Lossless duality.
- **Frankl-relevant normal form (UC9 §1.2).** WLOG: $[n]\in\mathcal F$ (else dual Frankl trivial, $[n]\setminus M_{\mathcal F}$ is all-rare). WLOG: $\bigcap\mathcal F=\emptyset$ (else $\bigcap\mathcal F$ contains an element abundant on the union side). So $\mathcal F$ is a meet-sublattice of $2^{[n]}$ with maximum $[n]$ and minimum $\emptyset$.
- **Slice statistics (UC9 §1.3).** $f_x:=|\mathcal F_x|$ (sets containing $x$); $f_x^c:=|\mathcal F_{\bar x}|$ (rarity). $\sum_x f_x^c=n|\mathcal F|-\sum_A|A|$. **Frankl (intersection-closed):** $\max_x f_x^c\ge|\mathcal F|/2$. **Trivial regime:** $\overline{\|A\|}\le n/2\Rightarrow$ trivial. **Non-trivial Frankl regime:** $\overline{\|A\|}>n/2$.

### The cone embedding (UC9 §2)

- **Cone relation.** $\omega_S:=\{(n+1,b):b\in S\}\subseteq[n+1]^2$ for $S\subseteq[n]$. The partial order on $[n+1]$ in which $n+1$ is below exactly $S\subseteq[n]$, and $[n]$ is an antichain. $\omega_\emptyset=\emptyset$ (empty relation, *not* in $\mathrm{PPF}_{n+1}$). $\omega_{[n]}=\omega_n$ (the F18 cone element).
- **Cone embedding (UC9 Defn §2.1).** $\iota_n^{\mathrm{cone}}:2^{[n]}\setminus\{\emptyset\}\to\mathrm{PPF}_{n+1}$, $S\mapsto\omega_S$. $\emptyset$ excluded (would map to empty relation); recover formally as $\widehat 0$ adjoined when needed for Möbius.
- **Properties (UC9 Lemmas 2.1–2.5).** Order-preserving, meet-preserving ($\omega_{S\cap T}=\omega_S\cap\omega_T$ when $S\cap T\ne\emptyset$), join-preserving, $S_n$-equivariant ($S_n\subseteq S_{n+1}$ fixing $n+1$). Image = $(\omega_n\!\downarrow)\setminus\{\emptyset\}$ — the principal order ideal of $\omega_n$ in $\mathrm{PPF}_{n+1}$, a Boolean sub-lattice of size $2^n-1$.
- **Uniqueness (UC9 Theorem 2.4).** $\iota_n^{\mathrm{cone}}$ is the unique embedding satisfying jointly: (a) $S_n$-equivariance, (b) meet-preservation, (c) F18-natural ($\iota([n])=\omega_n$), (d) image is principal ideal. Pinned by the four constraints together.
- **Embedded family.** $\widetilde{\mathcal F}=\{\omega_S:S\in\mathcal F,S\ne\emptyset\}\subseteq(\omega_n\!\downarrow)\setminus\{\emptyset\}$. In §1.2 normal form: $\widetilde{\mathcal F}$ has maximum $\omega_n$, minimum determined by smallest non-empty element of $\mathcal F$, intersection-closed in $\mathrm{PPF}_{n+1}$ (in the strong sense: $x,y\in\widetilde{\mathcal F}$ and $x\cap y\ne\emptyset$ ⟹ $x\cap y\in\widetilde{\mathcal F}$).
- **Direct verification (UC9 §2.3).** Antichain on $[n]$ embeds correctly; 2-chain / N-poset NOT in image (no internal-$[n]$ relations); empty / total relations excluded by $\mathrm{PPF}_{n+1}$ definition; $\omega_n$ is non-extremal in $\mathrm{PPF}_{n+1}$ (predecessors = embedded image, successors = extensions). No shortcuts invoked.

### F17+F18 cohomology, restated (UC9 §3)

- **Fact F (the master black-box theorem).** $\widetilde H^k(\Delta_n;\mathbb Q)=\mathrm{sgn}_{S_n}$ for $k=n-2$, zero else, all $n\ge 3$. Top-degree concentrated, sign-rep, unconditional. Source: F17 (mg-4d3a) + F18 (mg-d039) + F10 (mg-8216) inductive closure on Hyp.
- **Fact F at $n+1$.** $\widetilde H^k(\Delta_{n+1};\mathbb Q)=\mathrm{sgn}_{S_{n+1}}$ for $k=n-1$, zero else. The cohomology of the embedding *target* $\mathrm{PPF}_{n+1}$.
- **The degree-shift engine.** $\widetilde H^k(\Delta_{n+1})\cong\widetilde H^{k-1}(\Delta_n)\cong\cdots\cong\widetilde H^1(\Delta_3)$, all $\mathrm{sgn}$-reps of the appropriate $S_m$. Daniel's "cohomology type is $\mathrm{Pos}_{n-1}$" reads as: top class on $\mathrm{Pos}_{n+1}$ at degree $n-1$ is iterated-F18-lift of $\mathrm{Pos}_{n-1}$'s top class at degree $n-3$.
- **Branching (D-2).** $\mathrm{sgn}_{S_{n+1}}\downarrow_{S_n}=\mathrm{sgn}_{S_n}$. Sign-rep restricts to sign-rep.
- **Punctured-ideal $\simeq S^{n-2}$ (D-4).** $(\omega_n\!\downarrow)\setminus\{\emptyset,\omega_n\}\cong 2^{[n]}\setminus\{\emptyset,[n]\}$ has order complex $\simeq S^{n-2}$, with $S_n$-action giving $\widetilde H^{n-2}(S^{n-2})=\mathrm{sgn}_{S_n}$ rationally.

### Pullback / relative cohomology (UC9 §4)

- **Embedded image contractible (Lemma 4.1).** $\Delta(\mathrm{im})\simeq$ point (Boolean cube with maximum $\omega_n$ as cone apex).
- **Direct pullback zero (Cor 4.2).** $(\iota_n^{\mathrm{cone}})^*=0:\widetilde H^k(\Delta_{n+1})\to\widetilde H^k(\Delta(\mathrm{im}))=0$, every $k$. **THE U1 WALL.**
- **Survival theorem (Theorem 4.3).** $\widetilde H^k(\Delta_{n+1},\Delta(\mathrm{im}))\cong\widetilde H^k(\Delta_{n+1})$, every $k$. Top class survives unchanged in relative cohomology.
- **Family contractibility (Lemma 4.4).** $\Delta(\widetilde{\mathcal F})\simeq$ point for every intersection-closed $\mathcal F$ with $[n]\in\mathcal F$.
- **Family-relative survival (Theorem 4.5).** $\widetilde H^k(\Delta_{n+1},\Delta(\widetilde{\mathcal F}))\cong\widetilde H^k(\Delta_{n+1})$, every $k$. **Relative cohomology is family-independent on the contractibility class.** Family info not visible to direct relative cohomology.

### The three bridges (UC9 §5)

- **Slice setup (Lemma 5.1).** $\omega_n^{(x)}=\omega_n\setminus\{(n+1,x)\}$ for $x\in[n]$. Vertex-slice cover $(\omega_n\!\downarrow)\setminus\{\omega_n\}=\bigcup_x(\omega_n^{(x)}\!\downarrow)$.
- **Frankl in slice dialect (Lemma 5.2).** $f_x^c=|\widetilde{\mathcal F}\cap(\omega_n^{(x)}\!\downarrow)|$. Frankl ⟺ $\max_x f_x^c\ge|\widetilde{\mathcal F}|/2$.
- **Bridge A (vertex-slice MV).** Mayer–Vietoris spectral sequence on the family cover $\{\widetilde{\mathcal F}\cap(\omega_n^{(x)}\!\downarrow)\}_x$. $E_1^{p,q}$ spread over $|I|=p+1$ subsets; intersection $(\omega_{[n]\setminus I}\!\downarrow)$ is contractible for $I\subsetneq[n]$, empty/$\widehat 0$ for $I=[n]$. Coupling to F17+F18 top class via inclusion $\Delta(\widetilde{\mathcal F})\hookrightarrow\Delta_{n+1}$ + survival theorem. **UC10 mandate: degeneration condition under intersection-closure.**
- **Bridge B (Möbius / Euler / deletion-contraction).** Hall: $\mu_{\mathrm{PPF}_{n+1}^*}(\widehat 0,\widehat 1)=\widetilde\chi(\Delta_{n+1})=(-1)^{n-1}$ unconditional from F17+F18. $\mu_{\widetilde{\mathcal F}^*}(\widehat 0,\widehat 1)=\widetilde\chi(\Delta(\widetilde{\mathcal F}))=0$ from family contractibility (Lemma 4.4). Discrepancy $(-1)^{n-1}$ accumulates in $\mathrm{PPF}_{n+1}\setminus\widetilde{\mathcal F}$ via Crapo–Rota deletion-contraction. **UC10 mandate: closed-form deletion-contraction recursion organised by vertex-slice cover, evaluated against $(-1)^{n-1}$.**
- **Bridge C (equivariant sgn-projection on $S^{n-2}$).** Branching $\mathrm{sgn}_{S_{n+1}}\downarrow_{S_n}=\mathrm{sgn}_{S_n}$ + structural sphere ${\Delta(\mathrm{im}\setminus\{\omega_n\})\simeq S^{n-2}}$ with $\mathrm{sgn}_{S_n}$ on top. Equivariant Euler characteristic: $\langle\chi^{S_n}(S^{n-2}),\mathrm{sgn}_{S_n}\rangle=(-1)^{n-2}$. Decompose against $\widetilde{\mathcal F}\setminus\{\omega_n\}$ + complement; project to $\mathrm{sgn}_{S_n}$; slice statistics enter as cycle weights. **UC10 mandate: equivariant Lefschetz-trace + averaging-over-intersection-closure (the non-trivial step is intersection-closure NOT being $S_n$-invariant on $\widetilde{\mathcal F}$).**

### Compliance with Daniel's hard constraints (UC9 §0.3, §5.6, §6)

- **NOT factorial.** ✓ Only $S_n$-action via additive embedding $S_n\subseteq S_{n+1}$ (cone-stabilizer). No factorial decomposition; no Specht-module / character-table; no factorial-cohomology.
- **NOT functorial.** ✓ Three bridges are spectral / numerical / equivariant arguments on cohomology classes per family, never functors (intersection-closed) → (cohomology classes). UC9 §6.2 records the U1-shaped wall as exactly the failure-of-functoriality for the *obvious* morphism, with the dissolves being non-functorial bridges.
- **Paper-and-pencil first.** ✓ No Lean; no axioms; no computation.
- **Cumulative state doc.** ✓ This file.
- **Verify embedding directly.** ✓ §2.3 — no shortcuts; antichain / 2-chain / N-poset / empty / total / cone $\omega_n$ all directly verified.

### U1 obstruction (UC9 §6.2)

- **mg-26fc U1 (F-series).** "No operational bridge between BK spectral data and $\Delta(\mathrm{PPF}_n)$ cohomology — obvious morphism is identically zero or otherwise degenerate."
- **UC9 wall (cohomology dialect of U1).** "No operational bridge between intersection-closed $\mathcal F$ and F17+F18 cohomology of $\Delta(\mathrm{PPF}_{n+1})$ via cone-embedding pullback — direct pullback morphism is identically zero, family-pair LES is family-independent on contractibility class."
- **Structural identity.** Same wall in different dialects. Both arise from the *contractible-probe phenomenon*: any contractible probe (BK-spectral-sheaf in F28 dialect; embedded Boolean image in UC9 dialect) sees zero through the obvious pullback into the F17+F18 cathedral.
- **Dissolve.** Non-functorial bridge that goes around the obvious morphism. F29: Čech-2-cocycle on $\mathrm{PPF}_n$ from bad-cut-local biases. UC9: Bridges A / B / C of §5. All non-functorial.

### Trust-surface impact (UC9 §6.6)

- **NONE.** Paper-and-pencil only; no new axioms; no Lean changes; no computation; no engine port (E1–E3 / C1–C5 untouched); 1/3-2/3 critical path (`one_third_width_three`) untouched. The single load-bearing trust input is the unconditional F17+F18 cohomology (mg-4d3a + mg-d039), itself harness-verified (F17 21/21; F18 43677/43677).

---

## Open threads / what a UC10 (or Session 2) would do

UC9's own scope is **discharged** (the four-step strategy is set up through step 3 unconditionally; step 4 is identified with three concrete bridges, each precisely-mandated for a UC10 session). The forward work:

- **UC10 primary — Bridge B (Möbius / Euler / deletion-contraction).** The ranked-most-tractable bridge. Mandate: carry out the Crapo–Rota deletion-contraction on $\mathrm{PPF}_{n+1}^*$ relative to $\widetilde{\mathcal F}$, organised by the vertex-slice cover $\{(\omega_n^{(x)}\!\downarrow)\}_x$, with the F17+F18 input $(-1)^{n-1}$ as the global Möbius sign anchor. Likely outcome: AMBER-residual-pinned with a quantitative slice-profile bound.
- **UC10 secondary — Bridge C (equivariant sgn-projection on $S^{n-2}$).** Mandate: equivariant Lefschetz-trace + Burnside averaging on $S^{n-2}$, with the non-trivial step being intersection-closure NOT being $S_n$-invariant — needs care in the averaging. Likely outcome: GREEN-quantitative if averaging works; RED-averaging-fails if not.
- **UC10 tertiary — Bridge A (vertex-slice MV spectral sequence).** Mandate: degeneration condition under intersection-closure (or supplied by UC1–UC8 native cathedral, e.g. UC6 Thm 3.1 deficiency contraction). Most powerful if it works; technically demanding. Likely outcome: AMBER-degeneration-conditional-on-extra-structure.
- **UC10 fallback — combine bridges with UC1–UC8 native results.** If a single bridge is RED, try combining with UC8's cross-fibre persistence or UC6's deficiency contraction as the missing structural input. Specifically: Bridge A's degeneration condition might be supplied by UC6 Cor 3.2 (deficiency anti-monotonicity) read on the embedded family.
- **UC11+ — if all three bridges RED:** the strategy is genuinely walled; pivot to a fourth mechanism (e.g. F-series internal cofiber Morse $M_{\mathrm{rel}}^{\mathrm{eq}}$ pulled back to embedded image — but this breaches UC9's "F17+F18 black-box only" discipline and is a separate ticket).

Independence from F29: UC10 can run before F29 closes (both are independent of each other; F17+F18 input is unconditional regardless). If F29 GREENs first, UC10 has a tested template (Čech-cohomology dissolve of U1) to inform bridge selection. If F29 REDs first, UC10 can still GREEN — the embedding mechanism is structurally distinct from Čech assembly.

UC9 does not touch the dead routes (UC1 §8.4: E1–E3 / C1–C5) and is independent of the 1/3-2/3 critical path. The cross-cathedral consumption of F17+F18 is single-and-clean: any future revision of F17+F18 would require revising UC9, but F17+F18 is itself unconditional within the F-series cathedral.

---

*Cumulative state doc for mg-96a6 (UC9). Session 1 DONE — verdict AMBER-cohomology-pulls-back-cleanly-but-Frankl-extraction-needs-UC10. Branch `polecat-cat-mg-96a6`. Cone embedding $\iota_n^{\mathrm{cone}}:S\mapsto\omega_S$ pinned uniquely (Theorem 2.4); F17+F18 black-boxed as Fact F at $n+1$; direct pullback zero (Cor 4.2 — the U1 wall); family-relative survival (Theorem 4.5 — family-independent on contractibility class); three non-functorial bridges (A vertex-slice MV, B Möbius/Euler/deletion-contraction, C equivariant sgn-projection on $S^{n-2}$) surfaced for UC10 with explicit mandates and B → C → A tractability ranking; U1-shaped wall structurally identical to F-series U1 (mg-26fc), dissolved by non-functorial bridges. Trust-surface impact: NONE. No new axioms; no Lean changes; no computation; no engine port. The 300k cap was not approached.*
